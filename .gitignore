# Hugo
/public/
/resources/_gen/
/.hugo_build.lock

# Python
.venv/
__pycache__/
*.pyc

# OS
.DS_Store

# WordPress export (not needed in repo)
wordpress-export.xml

# Image working files (candidates, collage variants) — kept out of git,
# but in the repo dir so they survive /tmp cleanup
/image-workshop/
